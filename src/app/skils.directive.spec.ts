import { SkilsDirective } from './skils.directive';

describe('SkilsDirective', () => {
  it('should create an instance', () => {
    const directive = new SkilsDirective();
    expect(directive).toBeTruthy();
  });
});
